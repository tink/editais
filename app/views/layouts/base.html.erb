<!DOCTYPE html>
<html>
  <head>
    <title>Editais Públicos</title>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <%= stylesheet_link_tag %w(reset style scaffold forms) %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
  </head>
  <body>
    <div id=main>
      <div id=cabecalho>
        <div id=logo>
          <%= link_to raw(image_tag('logo.png', :alt => 'Editais Públicos')), root_url %>
        </div>
      </div>
      <% if yield(:menu) %>
        <div id=menu>
          <%= yield(:menu) %>
        </div>
      <% end %>
      <div id=corpo>
        <div id="principal" class="<%= local_assigns[:width] || 'split' %>">
          <div id=flashes>
            <%= flash_message %>
          </div>
          <%= yield %>
        </div>
        <% if yield(:sidebar).present? || yield(:top_sidebar).present? %>
          <div id=sidebar>
            <%= yield :top_sidebar %>
            <%= yield :sidebar %>
          </div>
        <% end %>
        <div class="clear"></div>
      </div>
      <div id=rodape>
        Editais Públicos é um serviço da tink! Para maiores informações entre em contato pelo e-mail editaispublicos@tink.com.br
      </div>
    </div>
  </body>
</html>
