<%= stylesheet_link_tag 'empresas/index' %>

<h1>Editais abertos</h1>
<ul>
  <% @editais.each do |edital| %>
    <li>
      <h2><%= link_to edital.nome, edital %></h2>
      <p class="date"><%= l edital.data_publicacao %> até <%= l edital.data_limite %></p>
      <p class="resumo"><%= edital.resumo %></p>
    </li>
  <% end %>
</ul>
